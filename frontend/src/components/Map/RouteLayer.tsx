// Placeholder component for route visualization on map
// Will be implemented with TomTom Routing API integration

interface RouteLayerProps {
  routes?: any[]; // TODO: Define proper route type
}

const RouteLayer = ({ routes = [] }: RouteLayerProps) => {
  // TODO: Render routes on TomTom map
  return null;
};

export default RouteLayer;
